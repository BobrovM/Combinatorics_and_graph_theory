#include <iostream>
//#include <queue>

using namespace std;

int main()
{
    int ArrayA[21]{ 0, 1 };
    int ArrayB[21]{ 0, 1 };

    int N;
    cin >> N;

    //for(int Index; Index < N)
}
    /*queue<int> Queue;
    Queue.push(1);
    Queue.push(2);

    int N;
    cin >> N;
    N--;

    for (int Index = 0; Index < N; Index++)
    {
        int Temp = Queue.front();
        Queue.pop();
        Queue.push(Temp + Queue.front());
    }

    Queue.pop();
    cout << Queue.front();*/